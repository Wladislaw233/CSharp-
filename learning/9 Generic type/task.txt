● В папке Storages добавляем интерфейс IStorage<T> описывающий
хранилище. Интерфейс содержит методы Add(T item), Update(T
item), Delete(T item).
● В папке Storages добавляем интерфейс “IClientStorage”
описывающий хранилище клиентов. Интерфейс имплементирует
“IStorage”, содержит методы - AddAccount, UpdateAccount,
DeleteAccount и свойство Dictionary<Client, List<Account>> Data.
● В созданном ранее хранилище клиентов имплементировать
интерфейс “IClientStorage ” и реализовать его методы.
● В классе “ClientService”, вместо ранее применяемого типа
“ClientStorage” применяем “IClientStorage” (поле и конструктор).
● Подкорректировать все затронутые изменениями тесты таким
образом, чтобы они вновь функционировали.
● Перестроить архитектуру работы с сотрудником банка базируясь на
примере с клиентами.
Дополнительное задание:
В сервисе “BankService” реализуем методы:
а) AddBonus принимает на вход наследника класса Person и
добавляет ему бонус;
б) AddToBlackList<T>(T person) - добавляет сущность в список
хранящийся в этом же классе;
в) IsPersonInBlackList<T>(T person) - проверяет присутствует
ли сущность в черном списке